# 📜 手順書生成アプリケーション (ManualAI) 変更・機能更新履歴

本ドキュメントは、手順書生成アプリケーション（ManualAI）の機能追加、不具合修正、およびバージョンアップの全履歴をまとめたログです。

---

## 🚀 最新の更新（v1.6.0） (2026-08-07)

### 📄 PDF・印刷時の特定位置改ページ制御機能
- **明示的改ページ記法のサポート**: Markdown 本文内に `<!-- pagebreak -->` または `[pagebreak]` または `<div class="page-break"></div>` を記述することで、PDF書き出し・印刷時にその指定位置で確実に強制改ページを実行するよう対応しました。
- **エディタ補助ボタン「改ページを挿入」**: Markdown エディタのツールバーに「📄 改ページを挿入」ボタンを新設し、ワンクリックで強制改ページタグを挿入できるように改善しました。
- **リアルタイムプレビューでの視覚的表示**: 編集画面の印刷プレビューペーパー上でも改ページ位置が破線バッジ「📄 改ページ (Page Break)」として表示され、位置を一目で確認・調整できるようにしました。

### 🎥 動画シーク配信 (HTTP 206 Partial Content) & コントロール最適化
- **HTTP Range Requests (206 Partial Content) 対応**: FastAPI バックエンドで動画配信時の Range Header に対応し、ブラウザ（Chrome/Edge/Safari）からの指定秒数へのタイムラインシークを完全に有効化しました。
- **非同期シーク保護ガード (`seeking` / `seeked`)**: フロントエンドにてシーク処理中およびデコード完了待ちの間の `timeupdate` 干渉を完全にブロックし、シークバーが元の再生位置へ引き戻される問題を解消しました。
- **描画エリアの最適化**: 動画プレイヤーの二重コントロール表示を統合・整理し、カード下部のレイアウト見切れを修正しました。

### 🖼 画像編集保存の0秒即時反映 (キャッシュバスター機構)
- **動的キャッシュバスターの導入**: 画像編集保存時に動的タイムスタンプ（`?v=${timestamp}`）をコンポーネント全体および Markdown 本文内の画像クエリに即時バインドし、保存ボタンを押した瞬間にサムネイルおよびプレビューが0秒で最新化されるよう改善しました。

### 🔍 使用画像一覧サムネイルの画面90%超拡大プレビュー機能
- **専用プレビューマーク (🔍) の配置**: 各サムネイルの右上角に操作を邪魔しないプレビューマークを新設し、ホバー時のみ拡大表示をトリガーするように設計しました。
- **画面全体の90%（90vw / 88vh）フルフレーム超拡大**: ホバー時にブラウザ画面の90%領域（枠目一杯）まで画像自体を限界拡大表示し、元画像解像度に関わらず細かい文字や図形注釈の内容を原寸大〜巨大画面でクリアに確認できるようにしました。

### ✍️ エディタのカーソル位置ピンポイント挿入 & スクロール自動保持
- **カーソル位置挿入**: 画像挿入や改ページ挿入時、本文末尾ではなく現在のカーソル（キャレット）位置へピンポイントで挿入するよう改善しました。
- **スクロール位置の完全保持**: 挿入時にエディタのスクロール位置（`scrollTop`）を維持し、ブラウザの自動フォーカス移動による画面位置ジャンプ・見失いを防止しました。

---

## 📦 バージョン 1.5.0 (2026-07-31)

### 🖼 編集時の任意画像追加・アップロード機能
- **ローカル画像ファイルの自由な追加**: 動画から抽出した静止画に加え、PC内の任意の画像ファイル（PNG, JPG, WebPなど）を手順書の編集時に自由に追加・アップロードできるよう拡張しました。
- **ワンクリック本文挿入 & 画像加工**: アップロードした画像も既存の切り出し画像と同様に、ワンクリックで Markdown 本文へ配置できるほか、インライン画像編集モーダル（切り抜き・矢印/図形注釈・リサイズ）での加工や削除が可能です。
- **識別バッジによる視覚的区別**: 使用画像一覧において、動画切り出し画像（タイムスタンプバッジ表示・動画シーク機能）と追加画像（「アップロード」緑バッジ表示）を直感的に識別できるよう表示を更新しました。

### 🗄 既存・新規環境に対応した動的DBマイグレーション
- **自動マイグレーション処理 (`run_migrations`)**: アプリ起動時にデータベース構造を自動検査し、`manual_images` テーブルへの `image_type` カラム追加および `timestamp` カラムの NULL 許容化を安全に自動適用します。既存稼働中のデータベースデータを保持したまま、新機能へのアップグレードを安全に行うことができます。

---

## 📦 バージョン 1.4.1

### 🤖 Gemini API エラーメッセージの詳細化・具体化
- **具体的原因の判定表示**: Gemini API リクエスト失敗時に従来の汎用エラーではなく、原因に応じた明確な日本語エラーメッセージを画面上に出力するよう改善しました。
  - **クレジット不足 / クォータ制限 (429 / ResourceExhausted)**: レートリミットやクレジット不足を明示。
  - **認証エラー / APIキー無効 (401 / 403 / PermissionDenied)**: APIキー設定の確認を促すメッセージを出力。
  - **APIサーバー障害 (500 / 503 / ServerError)**: Google側の時限的障害を明示。
  - **通信・タイムアウト**: ネットワーク接続やプロキシ確認メッセージを出力。

### 🌐 デフォルトプロンプトの日本語出力強制指示
- デフォルトのAIメタプロンプトを改修し、入力動画の音声や字幕が他言語であっても、作成される手順書のタイトル・本文・ステップ解説・タイムスタンプ注釈が**必ず自然な日本語で生成される言語制約**を強化・徹底しました。

### 📐 画像編集モーダルでの「100% (元サイズに戻す)」オプション追加
- 静止画編集モーダルの「サイズ調整」ツールバーに **`100%`** ボタンを追加し、75% や 50% に縮小した後でも初期解像度へワンタップで綺麗に復元できるよう改善しました。

---

## 📦 バージョン 1.4.0 (2026-07-27)

### 📄 PDF書き出し時の画像自動スケーリング & ページまたぎ防止
- **画像高さ自動制限 (`max-height: 105mm; object-fit: contain;`)**:
  - A4 ページの縦幅を超えかねない巨大画像が挿入された場合でも、ページ枠やテキスト量に合わせて自動的に縦横比を保って縮小されるよう PDF/HTML 印刷スタイルを強化しました。
- **1手順ブロックのページまたぎ防止 (`.step-block` & `break-inside: avoid;`)**:
  - 見出し＋説明テキスト＋画像を 1 つの手順ブロックとして自動ラッパーし、ページ境目で不自然に泣き別れ（分割）せず、ブロック全体が丸ごと次のページへ繰り越される改ページ制御を実装しました。

### 🎨 静止画の手動リサイズ機能 (75% / 50% 縮小)
- 静止画編集モーダルに、画像のサイズを直感的に縮小できる **`75%`** および **`50%`** ボタンを追加しました。

---

## 📦 バージョン 1.3.0 (2026-07-27)

### ✨ AIプロンプト指示による本文修正・補正機能
- **対話型 AI リライトパネルの設置**:
  - Markdown エディタ直下に「✨ AIアシスタントによる手順書補正」パネルを新設。
  - 「注意点を太字で追加」「初心者向けに平易化」「箇条書きで整理」「誤字脱字校正」などの自然言語指示を入力して、画像タグを保持したまま本文をリアルタイムに書き直す機能を実装しました。
- **ワンタップ・クイック指示ボタン**:
  - よく使われる補正指示をワンクリックで入力欄にセットできるプリセットボタンを設置しました。

### 🔘 書き出しドロップダウンの表示安定化
- 「書き出し」ボタンクリックによるトグル状態保持（`isExportDropdownOpen`）および `:hover` すり抜け防止用の透明ブリッジを追加し、メニューが意図せず閉じる不具合を修正しました。

---

## 📦 バージョン 1.2.0 (2026-07-23)

### 🤖 手順書生成時の AI モデル選択機能
- 手順書作成画面（`http://localhost:4202/create`）にて、解析に使用する Gemini モデルをユーザーが自由に選択できる UI（モデル選択カード）を追加しました。
  - **Gemini 3.5 Flash** (`gemini-3.5-flash`) 【デフォルト / 推奨】
  - **Gemini 3.6 Flash** (`gemini-3.6-flash`) 【最新 Flash】
  - **Gemini 3.5 Flash Lite** (`gemini-3.5-flash-lite`) 【超高速】
  - **Gemini 3 Pro Preview** (`gemini-3-pro-preview`) 【高精度 Pro】
  - **Gemini 2.5 Pro** (`gemini-2.5-pro`) 【Pro 安定版】

---

## 📦 バージョン 1.1.0 (2026-07-23)

### ✏️ 静止画のインライン編集機能 (切り抜き & 注釈図形)
- 抽出されたキーフレーム画像に対し、ブラウザ上で直感的に操作できるキャンバスエディタモーダルを実装しました。
  - ✂️ **画像の切り抜き (Crop)**: ドラッグ選択によるトリミング。
  - 🔴/🟦/➡️ **図形挿入**: 円・矩形・矢印の注釈描画。
  - 🎨 **カラー & 線幅カスタマイズ**: 6色・3段階の太さ変更、Undo (取り消し) 機能。
  - 💾 **サーバー直接上書き更新**: 編集画像がプレビューやエクスポート文書へ即座に反映。

### 📱 サイドバー直近手順書の表示
- 画面左側の共通サイドバーナビゲーションに、最近作成・更新された手順書（最大5件）を常時表示し、ワンクリックで移動できるクイックアクセス機能を追加しました。

### 🏷 アプリケーションタイトルの更新
- タイトル表記を「Frontent」から正式名称 **`ManualAI`** へ変更しました。

### 🌐 スタンドアロン HTML 出力 (Base64 画像完全埋め込み)
- HTML エクスポート時にローカル画像ファイルを Base64 Data URI として自動埋め込み、単体ファイルとして機能するポータブルな HTML 出力に対応しました。

---

## 📦 バージョン 1.0.0 (2026-07-21)

### 🎉 初期リリース
- **動画解析 & 手順書自動生成**: アップロード動画から Google Gemini API を用いた音声文字起こし・操作ステップ抽出・Markdown生成。
- **OpenCV フレーム自動抽出**: キーフレーム（タイムスタンプ）の自動画像切り出し。
- **対話型2カラムエディタ**: 動画プレイヤーシーク連動、タイムラインキャプチャ、リアルタイム印刷プレビュー。
- **多形式エクスポート**: WeasyPrint を用いた高品質 PDF、HTML、Markdown 出力。
- **Docker Compose マルチコンテナ環境**: Angular 18 + FastAPI + PostgreSQL 16 構成。
